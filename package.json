{
  "name": "modernize-app-with-copilot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A two-track GitHub Copilot modernization exercise.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-repository.mjs",
    "lab": "node scripts/lab.mjs",
    "checkpoint": "node scripts/checkpoint.mjs",
    "compile:review": "node scripts/compile-review.mjs",
    "verify": "npm test && npm run check"
  },
  "devDependencies": {
    "yaml": "2.9.0"
  }
}
